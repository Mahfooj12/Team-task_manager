{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "nixPkgs": ["nodejs_20", "npm"],
    "phases": {
      "setup": {
        "nixPkgs": ["nodejs_20", "npm"]
      }
    }
  },
  "deploy": {
    "startCommand": "npx serve -s dist"
  }
}
