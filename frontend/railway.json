{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build",
    "nixPkgs": ["nodejs_20"]
  },
  "deploy": {
    "startCommand": "npx serve -s dist",
    "healthcheckPath": "/"
  }
}
